# Production discover users (phase 1) — same-origin proxy via Next rewrites
NEXT_PUBLIC_USE_MOCK=false
NEXT_PUBLIC_API_URL=/api/
BACKEND_API_ORIGIN=https://amctag-ehkini.38f0fz.easypanel.host

# Direct browser URL (only if CORS allows; otherwise keep /api/ + rewrites in next.config.mjs)
NEXT_PUBLIC_API_URL=https://amcserver.com/app/taaruf/public/api/v2/
